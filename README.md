# docker-dotnet-console

1. Build image
    ```bash
    docker image build -t hello-net .
    ```

2. Check image
    ```bash
    docker image ls
    ```

3. Run container
    ``` bash
    docker container run hello-net
    ```